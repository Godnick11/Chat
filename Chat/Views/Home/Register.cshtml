@model Chat.Models.RegisterUserViewModel

@{
    Layout = "~/Views/Shared/Layout.cshtml";
    ViewBag.Title = "Welcome!";
}

@section Header
{
    <div class="navbar">
        <div class="navbar-inner">
        </div>
    </div>
}

<div class="row">
    <div class="span4">
        @Html.ValidationSummary(excludePropertyErrors: true, message: string.Empty, htmlAttributes: new { @class = "text-error" })
    </div>
</div>

<div class="row">
    @using (Html.BeginForm("RegisterNewUser", "Home", FormMethod.Post, new { @class = "form-horizontal" }))
    {
        <div class="control-group">
            @Html.LabelFor(m => m.FirstName, htmlAttributes: new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(m => m.FirstName, htmlAttributes: new { placeholder = "first name" })
            </div>
            @Html.ValidationMessageFor(m => m.FirstName, validationMessage: string.Empty, htmlAttributes: new { @class = "text-error" })
        </div>
        <div class="control-group">
            @Html.LabelFor(m => m.LastName, htmlAttributes: new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(m => m.LastName, htmlAttributes: new { placeholder = "last name" })
            </div>
            @Html.ValidationMessageFor(m => m.LastName, validationMessage: string.Empty, htmlAttributes: new { @class = "text-error" })
        </div>
        <div class="control-group">
            @Html.LabelFor(m => m.Email, htmlAttributes: new { @class = "control-label" })
            <div class="controls">
                @Html.TextBoxFor(m => m.Email, htmlAttributes: new { placeholder = "email", type = "email" })
            </div>
            @Html.ValidationMessageFor(m => m.Email, validationMessage: string.Empty, htmlAttributes: new { @class = "text-error" })
        </div>
        <div class="control-group">
            @Html.LabelFor(m => m.Password, new { @class = "control-label" })
            <div class="controls">
                @Html.PasswordFor(m => m.Password, new { placeholder = "password" })
            </div>
            @Html.ValidationMessageFor(m => m.Password, validationMessage: string.Empty, htmlAttributes: new { @class = "text-error" })
        </div>
        <div class="control-group">
            @Html.LabelFor(m => m.RepeatPassword, new { @class = "control-label" })
            <div class="controls">
                @Html.PasswordFor(m => m.RepeatPassword, new { placeholder = "password" })
            </div>
            @Html.ValidationMessageFor(m => m.RepeatPassword, validationMessage: string.Empty, htmlAttributes: new { @class = "text-error" })
        </div>
        <div class="control-group">
            @Html.LabelFor(m => m.RememberMe, new { @class = "control-label" })
            <div class="controls">
                @Html.CheckBoxFor(m => m.RememberMe)
                <button type="submit" class="btn btn-success">Register and Login</button>
            </div>
        </div>
    }
</div>
